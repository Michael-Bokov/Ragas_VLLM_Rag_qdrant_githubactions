docker-compose up -d
3 containers (qdrant,vllm,fastapi service localhost:8080/docs)

<u>Ragas tests</u>
test folder -> create venv -> pip install -r requirements.txt -> cd .. (go to root folder) ->pytest tests/test_rag_quality.py
